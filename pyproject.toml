[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trust-triage"
version = "0.1.0"
description = "TRUST-TRIAGE static malware triage components"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

